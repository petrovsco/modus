{
  "version": 2,
  "description": "Source of truth for /modus:init. Each entry is an opt-in config. category: plugin = enable via enabledPlugins; rule = one @import line in the project CLAUDE.md (synced by the core modus plugin); settings/mcp/environment/convention = merged or applied per the install steps. `scope` = where it lands (project | global | machine). `recommendWhen` lets /modus:init pre-check relevant items after sniffing the project.",
  "entries": [
    {
      "id": "session-flow",
      "title": "session-flow plugin (context guard + memory nudge + /roadmap)",
      "category": "plugin",
      "scope": "project",
      "summary": "PostToolUse context-budget guard (wrap-up nudge at ~180k/~195k, zero model tokens), a memory-review nudge after each git commit, and the /roadmap command over docs/roadmap/ task briefs.",
      "assets": ["plugins/session-flow"],
      "install": [
        "Merge {\"enabledPlugins\": {\"session-flow@modus\": true}} into <project>/.claude/settings.local.json (or settings.json to commit the choice).",
        "Create the docs/roadmap/ directory (where task briefs live).",
        "Remove legacy copies if present: .claude/hooks/context-guard.mjs, .claude/commands/roadmap.md, and the matching hook entries in settings.local.json.",
        "Restart the session so the plugin loads."
      ],
      "recommendWhen": "Project has a build + commit workflow and sessions get long.",
      "dependsOn": ["session-wrap-up"],
      "requiresSecret": false
    },
    {
      "id": "visual-iteration",
      "title": "visual-iteration plugin (Playwright MCP + workflow skill)",
      "category": "plugin",
      "scope": "project",
      "summary": "Playwright MCP server plus the visual-iteration skill: serve scratch HTML/SVG over HTTP, iterate with element screenshots, clean up after.",
      "assets": ["plugins/visual-iteration"],
      "install": [
        "Merge {\"enabledPlugins\": {\"visual-iteration@modus\": true}} into <project>/.claude/settings.local.json.",
        "Remove a legacy playwright block from <project>/.mcp.json if the plugin now provides it.",
        "Restart the session so the plugin loads."
      ],
      "recommendWhen": "Web / UI projects, or anything with visual output to iterate on.",
      "dependsOn": [],
      "requiresSecret": false
    },
    {
      "id": "session-wrap-up",
      "title": "Session context wrap-up protocol (house rule)",
      "category": "rule",
      "scope": "project",
      "summary": "Work in atomic build-passing units, checkpoint each, and hand off remaining scope to a docs/roadmap/ brief when the context window fills.",
      "assets": ["plugins/modus/rules/session-wrap-up.md"],
      "install": [
        "Ensure the core modus plugin is enabled at user scope (it syncs rules to ~/.claude/modus/rules/).",
        "Add to the project CLAUDE.md under '## House rules': @~/.claude/modus/rules/session-wrap-up.md",
        "If the rule body is pasted in CLAUDE.md from the pre-plugin era, replace the pasted section with the import line."
      ],
      "recommendWhen": "Paired with the session-flow plugin; any repo with long build sessions.",
      "dependsOn": ["session-flow"],
      "requiresSecret": false
    },
    {
      "id": "build-before-push",
      "title": "Build (not just typecheck) before pushing (house rule)",
      "category": "rule",
      "scope": "project",
      "summary": "Run the full build the deploy runs before committing/pushing.",
      "assets": ["plugins/modus/rules/build-before-push.md"],
      "install": [
        "Ensure the core modus plugin is enabled at user scope.",
        "Add to the project CLAUDE.md under '## House rules': @~/.claude/modus/rules/build-before-push.md"
      ],
      "recommendWhen": "Any project with a build/compile step that CI or the host runs.",
      "dependsOn": [],
      "requiresSecret": false
    },
    {
      "id": "direct-push",
      "title": "Direct-to-main push + auto-checkpoint (house rule)",
      "category": "rule",
      "scope": "project",
      "summary": "Push straight to main and auto commit+push finished, build-passing units without being asked. Opt-in per repo.",
      "assets": ["plugins/modus/rules/direct-push.md"],
      "install": [
        "ONLY for solo/trunk-based repos — confirm with the user first.",
        "Ensure the core modus plugin is enabled at user scope.",
        "Add to the project CLAUDE.md under '## House rules': @~/.claude/modus/rules/direct-push.md"
      ],
      "recommendWhen": "Solo repos without PR review or protected branches. Confirm first.",
      "dependsOn": ["build-before-push"],
      "requiresSecret": false
    },
    {
      "id": "npm-permissions",
      "title": "Safe permission allowlist",
      "category": "settings",
      "scope": "project",
      "summary": "Pre-allow low-risk, frequently-approved commands (e.g. `npm run *`) so the agent isn't prompted repeatedly.",
      "assets": ["configs/settings/permissions.json"],
      "install": [
        "Merge permissions.allow from configs/settings/permissions.json into <project>/.claude/settings.local.json.",
        "Optionally run /fewer-permission-prompts later to grow the list from your own transcripts."
      ],
      "recommendWhen": "Node/npm projects. Adapt the globs for other stacks.",
      "dependsOn": [],
      "requiresSecret": false
    },
    {
      "id": "global-settings",
      "title": "Global agent settings (model / effort / notifications)",
      "category": "settings",
      "scope": "global",
      "summary": "~/.claude/settings.json preferences: model, effortLevel=xhigh, push notifications on background-agent completion.",
      "assets": ["configs/settings/global-settings.json"],
      "install": [
        "Merge the keys from configs/settings/global-settings.json into ~/.claude/settings.json."
      ],
      "recommendWhen": "Set once per machine (global). Skip if already configured to taste.",
      "dependsOn": [],
      "requiresSecret": false
    },
    {
      "id": "mcp-supabase",
      "title": "Supabase MCP server",
      "category": "mcp",
      "scope": "project",
      "summary": "Inspect/query a Supabase project (tables, migrations, logs, advisors) from the agent.",
      "assets": ["configs/mcp/supabase.md"],
      "install": [
        "Add the supabase server block from configs/mcp/supabase.md to <project>/.mcp.json.",
        "Provide a real sbp_ access token (never commit it — gitignore .mcp.json or inject from env)."
      ],
      "recommendWhen": "Project uses Supabase.",
      "dependsOn": [],
      "requiresSecret": true
    },
    {
      "id": "git-ssh-windows",
      "title": "Git SSH push fix (Windows + Git Bash)",
      "category": "environment",
      "scope": "machine",
      "summary": "Point git at Windows-native ssh.exe globally, and keep the OpenSSH agent service running with the key loaded, so SSH pushes work from every shell in every repo.",
      "assets": ["configs/environment/git-ssh-windows.md"],
      "install": [
        "Verify C:/WINDOWS/System32/OpenSSH/ssh.exe exists.",
        "Run: git config --global core.sshCommand \"C:/WINDOWS/System32/OpenSSH/ssh.exe\"",
        "Drop any stale per-repo override: git config --local --unset core.sshCommand",
        "If pushes still hang: Set-Service ssh-agent -StartupType Automatic; Start-Service ssh-agent; ssh-add (type the key passphrase once)."
      ],
      "recommendWhen": "Windows machine where SSH git pushes fail/hang. Set once per machine.",
      "dependsOn": [],
      "requiresSecret": false
    },
    {
      "id": "memory-protocol",
      "title": "Auto-memory protocol",
      "category": "convention",
      "scope": "global",
      "summary": "File-based persistent memory kept outside the repo (~/.claude/projects/<slug>/memory/): one fact per file + a MEMORY.md index. Conventions for what to save and how.",
      "assets": ["configs/memory/README.md"],
      "install": [
        "This is a convention, not a copied file. Review configs/memory/README.md and follow it; the memory files are created on demand by the harness."
      ],
      "recommendWhen": "Any ongoing project where cross-session recall pays off.",
      "dependsOn": [],
      "requiresSecret": false
    }
  ]
}
