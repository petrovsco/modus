{
  "name": "modus",
  "description": "Core modus plugin: /modus:init (set up a repo from the catalog), /modus:capture (turn recent work or an idea into a reusable config), and the SessionStart sync that publishes house rules to ~/.claude/modus/rules/ for CLAUDE.md @imports.",
  "version": "1.0.0",
  "author": { "name": "Peter Shamatoff" }
}
